name: 2.1$(rev:.r)

trigger:
- master

resources:
- repo: self

variables:

  dockerRegistryServiceConnection: '72c293a5-902c-4f0f-b415-743aeff8b352'
  imageRepository: 'qboxcoreweb'
  containerRegistry: 'techdays.azurecr.io'
  dockerfilePath: '**/Dockerfile'
  tag: '$(build.buildnumber)'

stages:
- stage: Build
  displayName: Build

  jobs:  
  - template: build-qbox.yml

- stage: DeployTest
  displayName: Deploy Test Europe
  dependsOn: Build
  jobs:
  - deployment: Deploy
    displayName: Deploy
    pool:
      name: Local
    environment: 'qbox-test.qbox-test.techdays-eu'
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self          
          - template: deploy-qbox.yml
            parameters:
              namespace: 'qbox-test'
              kubernetesServiceConnection: 'qbox-test-techdays-eu'  
              ingressHostName: 'techdays.westeurope'

- stage: DeployTestUS
  displayName: Deploy Test US
  dependsOn: Build
  jobs:
  - deployment: Deploy
    displayName: Deploy
    pool:
      name: Local
    environment: 'qbox-test.qbox-test.techdays-us'
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self          
          - template: deploy-qbox.yml
            parameters:
              namespace: 'qbox-test'
              kubernetesServiceConnection: 'qbox-test-techdays-us'     
              ingressHostName: 'techdays.eastus2'
